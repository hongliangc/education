import { EnglishHub } from "@/components/games/english/EnglishHub";

// Standalone demo (no game HUD) for evaluating the scene-based speaking lesson — meet the words,
// listen & find, say it, swap the pattern, then role-play with the fairy — before wiring it into
// the graded platform. http://localhost:3000/english-demo
export default function EnglishDemoPage() {
  return <EnglishHub />;
}
